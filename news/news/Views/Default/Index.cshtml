@model IEnumerable<news.Models.Article>
@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model)
{

        <div class="container-narrow">
            <div class="row rowfix">
                <div class="col-sm-12 divnews">
                    <h2><strong>@item.Heading</strong></h2>


                    @if (@item.VideoAdress != null)
                    {
                        <iframe scrolling="no" src="@item.VideoAdress" class="picc"></iframe>

                    }

                    else if (@item.ImgAdress != null)
                    {

                        <img src="@item.ImgAdress" class="picc">

                    }

                    <div id="divtext">
                        <p>@item.Text</p>
                    </div>
                    <hr>
                    <p class="tagme"> By: @item.User.UserName @item.Date.ToShortDateString() Category: @*@item.Categories*@</p>
                </div>
            </div>
        </div>

}

